mapreduce:
- name: remove_bm
  mapper:
    input_reader: mapreduce.input_readers.DatastoreKeyInputReader
    handler: app.reduce.remove_bm
    params:
    - name: entity_kind
      default: models.Bookmarks

- name: reset_index
  mapper:
    input_reader: mapreduce.input_readers.DatastoreKeyInputReader
    handler: app.reduce.reset_index
    params:
    - name: entity_kind
      default: models.UserInfo

- name: reindex_all
  mapper:
    input_reader: mapreduce.input_readers.DatastoreKeyInputReader
    handler: app.reduce.reindex_all
    params:
    - name: entity_kind
      default: models.Bookmarks